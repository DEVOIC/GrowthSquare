
// eslint-disable-next-line @typescript-eslint/no-unused-vars
interface Course {
    _id: string
    title: string
    description: string
    instructor: string
    sections: string[]
    tags: string[]
    thumbnail: string
    studentEnrolled: string[]
    rating: number
    price: number
}